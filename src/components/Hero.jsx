import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <h2>Welcome to the Pet Store</h2>
            <p>Your one-stop shop for all pet needs</p>
        </section>
    );
};

export default Hero;
